import 'package:flutter/material.dart';

class IconButtonSizes {
  static const Size small = Size(32, 32);
  static const Size medium = Size(48, 48);
  static const Size large = Size(64, 64);
}

class AppSizes {
  static const double radius = 6.0;
  static const double padding = 12.0;
  static const double inputHeight = 42.0;
}
