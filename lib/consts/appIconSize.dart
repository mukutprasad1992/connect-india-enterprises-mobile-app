import 'package:flutter/material.dart';

class ElevatedButtonSizes {
  static const Size small = Size(100, 36); // Width = 100 pixels , Height = 36 pixels
  static const Size medium = Size(140, 48);
  static const Size large = Size(180, 56);
}

class TextButtonSizes {
  static const Size small = Size(80, 32); // Width = 80 pixels , Height = 32 pixels
  static const Size medium = Size(120, 44);
  static const Size large = Size(160, 52);
}

class AppSizes {
  static const double radius = 6.0;
  static const double padding = 12.0;
  static const double inputHeight = 42.0;
}
